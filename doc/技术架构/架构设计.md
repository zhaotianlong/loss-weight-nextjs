# 减肥训练营管理系统 - 架构文档

## 系统概述
这是一个完整的减肥训练营管理后台系统，支持多营地、多角色管理。

## 数据库 ER 图

```mermaid
erDiagram
    USER ||--o{ CAMP : manages
    USER ||--o{ PUBLIC_COURSE : instructs
    USER ||--o{ PRIVATE_COURSE : coaches
    USER ||--o{ RECIPE : creates
    
    CAMP ||--o{ ROOM : contains
    CAMP ||--o{ PUBLIC_COURSE : hosts
    CAMP ||--o{ CAMP_CHARGE_STANDARD : defines
    CAMP ||--o{ DAILY_MENU : provides
    CAMP ||--o{ RECIPE : uses
    
    ROOM ||--o{ BED : contains
    
    STUDENT ||--o{ STUDENT_PHYSICAL_INFO : has
    STUDENT ||--o{ STUDENT_CHECK_IN : makes
    STUDENT ||--o{ STUDENT_TUITION : pays
    STUDENT ||--o{ PRIVATE_COURSE : enrolls
    STUDENT ||--o{ PRIVATE_COURSE_ORDER : purchases
    
    BED ||--o{ STUDENT_CHECK_IN : assigns
    
    PUBLIC_COURSE ||--o{ COURSE_SCHEDULE : follows
    
    PRIVATE_COURSE ||--o{ PRIVATE_COURSE_ORDER : has
    
    CAMP_CHARGE_STANDARD ||--o{ STUDENT_TUITION : references
    
    STUDENT_TUITION ||--o{ TUITION_RENEWAL : extends
    
    RECIPE ||--o{ RECIPE_INGREDIENT : contains
    RECIPE ||--o{ DAILY_MENU : appears_in
    
    USER {
        string id PK
        string name
        string email UK
        string phone
        string password
        string role "admin|manager|coach|chef|cleaner|receptionist"
        string avatar
        boolean isActive
        datetime joinDate
        datetime createdAt
        datetime updatedAt
    }
    
    CAMP {
        string id PK
        string name
        string location
        string address
        int capacity
        string description
        string contactPhone
        string manager FK "User.id"
        datetime createdAt
        datetime updatedAt
    }
    
    ROOM {
        string id PK
        string campId FK
        string roomNumber
        int floor
        int beds
        string description
        boolean isActive
        datetime createdAt
        datetime updatedAt
    }
    
    BED {
        string id PK
        string roomId FK
        string bedNumber
        boolean isActive
        datetime createdAt
        datetime updatedAt
    }
    
    STUDENT {
        string id PK
        string name
        string gender "male|female"
        string phone
        string idNumber
        date birthDate
        string emergencyContact
        string emergencyPhone
        date joinDate
        string avatar
        datetime createdAt
        datetime updatedAt
    }
    
    STUDENT_PHYSICAL_INFO {
        string id PK
        string studentId FK
        date recordDate
        decimal height
        decimal weight
        decimal bmIndex
        decimal fatPercentage
        decimal muscleMass
        string notes
        datetime createdAt
        datetime updatedAt
    }
    
    STUDENT_CHECK_IN {
        string id PK
        string studentId FK
        string bedId FK
        datetime checkInDate
        datetime checkOutDate
        string status "checked_in|checked_out|no_show"
        string notes
        datetime createdAt
        datetime updatedAt
    }
    
    PUBLIC_COURSE {
        string id PK
        string campId FK
        string name
        string description
        string instructorId FK "User.id"
        int maxParticipants
        string location
        boolean isActive
        datetime createdAt
        datetime updatedAt
    }
    
    COURSE_SCHEDULE {
        string id PK
        string courseId FK
        int dayOfWeek "0-6"
        string startTime
        string endTime
        boolean repeating
    }
    
    PRIVATE_COURSE {
        string id PK
        string studentId FK
        string coachId FK "User.id"
        string title
        string description
        int totalSessions
        int completedSessions
        date startDate
        date endDate
        decimal price
        string status "active|completed|cancelled"
        string notes
        datetime createdAt
        datetime updatedAt
    }
    
    PRIVATE_COURSE_ORDER {
        string id PK
        string studentId FK
        string courseId FK
        datetime purchaseDate
        decimal totalAmount
        int sessionsIncluded
        string paymentStatus "pending|completed|failed|refunded"
        datetime paymentDate
        datetime createdAt
        datetime updatedAt
    }
    
    RECIPE {
        string id PK
        string campId FK
        string name
        string category "早餐|午餐|晚餐|加餐"
        decimal calories
        decimal protein
        decimal carbs
        decimal fat
        string chef FK "User.id"
        boolean isActive
        string imageUrl
        string instructions
        datetime createdAt
        datetime updatedAt
    }
    
    RECIPE_INGREDIENT {
        string id PK
        string recipeId FK
        string ingredient
        decimal quantity
        string unit
    }
    
    DAILY_MENU {
        string id PK
        string campId FK
        date date
        string breakfast FK "Recipe.id"
        string lunch FK "Recipe.id"
        string dinner FK "Recipe.id"
        string snack FK "Recipe.id"
        string notes
        datetime createdAt
        datetime updatedAt
    }
    
    CAMP_CHARGE_STANDARD {
        string id PK
        string campId FK
        string packageName
        int duration
        decimal price
        decimal discount
        string includes
        string description
        boolean isActive
        datetime createdAt
        datetime updatedAt
    }
    
    STUDENT_TUITION {
        string id PK
        string studentId FK
        string chargeStandardId FK
        decimal amount
        string paymentStatus "pending|completed|failed|refunded"
        datetime paymentDate
        date expiryDate
        string notes
        datetime createdAt
        datetime updatedAt
    }
    
    TUITION_RENEWAL {
        string id PK
        string tuitionId FK
        date renewalDate
        date newExpiryDate
        decimal amount
        string paymentStatus "pending|completed|failed|refunded"
        datetime paymentDate
        datetime createdAt
        datetime updatedAt
    }
```

## 系统架构图

```mermaid
graph TB
    Client["浏览器客户端"]
    
    subgraph Frontend["前端层 - Next.js"]
        Layout["全局布局"]
        Auth["身份认证页面"]
        Dashboard["仪表板"]
        StudentMgmt["学员管理"]
        CourseMgmt["课程管理"]
        MealMgmt["菜谱管理"]
        FeeMgmt["费用管理"]
        ReportMgmt["报表管理"]
    end
    
    subgraph API["API 层 - Next.js Route Handlers"]
        UserAPI["用户 API"]
        CampAPI["营地 API"]
        StudentAPI["学员 API"]
        CourseAPI["课程 API"]
        RecipeAPI["菜谱 API"]
        TuitionAPI["学费 API"]
        StatsAPI["统计 API"]
    end
    
    subgraph Business["业务逻辑层"]
        UserService["用户服务"]
        StudentService["学员服务"]
        CourseService["课程服务"]
        RecipeService["菜谱服务"]
        PaymentService["支付服务"]
        AnalyticsService["分析服务"]
    end
    
    subgraph Database["数据持久层"]
        DB[(数据库)]
        Cache["缓存 Redis"]
    end
    
    Client -->|HTTP/REST| Frontend
    
    Frontend -->|API Calls| API
    
    API -->|调用| Business
    
    Business -->|读写| Database
    Business -->|缓存| Cache
    
    Auth -->|验证| UserService
    
    Dashboard -->|查询| StatsAPI
    StatsAPI -->|聚合| AnalyticsService
    
    StudentMgmt -->|增删改查| StudentAPI
    StudentAPI -->|业务逻辑| StudentService
    
    CourseMgmt -->|增删改查| CourseAPI
    CourseAPI -->|业务逻辑| CourseService
    
    MealMgmt -->|增删改查| RecipeAPI
    RecipeAPI -->|业务逻辑| RecipeService
    
    FeeMgmt -->|增删改查| TuitionAPI
    TuitionAPI -->|支付处理| PaymentService
```

## 权限矩阵

```mermaid
graph LR
    Admin["管理员<br/>Admin"]
    Manager["经理<br/>Manager"]
    Coach["教练<br/>Coach"]
    Chef["厨师<br/>Chef"]
    Cleaner["保洁<br/>Cleaner"]
    Receptionist["前台<br/>Receptionist"]
    
    Admin -->|全部权限| All["所有功能"]
    Manager -->|营地管理<br/>员工管理<br/>报表查看| ManFunc["管理函数"]
    Coach -->|课程管理<br/>学员进度| CoachFunc["教练函数"]
    Chef -->|菜谱管理<br/>每日菜单| ChefFunc["厨师函数"]
    Cleaner -->|卫生记录| CleanFunc["保洁函数"]
    Receptionist -->|学员登记<br/>入住办理<br/>费用收取| FrontFunc["前台函数"]
```

## 关键业务流程

### 1. 学员入营流程

```mermaid
sequenceDiagram
    participant Student as 学员
    participant Receptionist as 前台
    participant System as 系统
    participant Database as 数据库
    
    Student ->> Receptionist: 申请入营
    Receptionist ->> System: 创建学员记录
    System ->> Database: 保存学员信息
    Receptionist ->> System: 选择房间/床位
    System ->> Database: 分配床位
    Receptionist ->> System: 记录入住信息
    System ->> Database: 保存入住记录
    System -->> Receptionist: 入营成功
    Receptionist -->> Student: 发放房间钥匙
```

### 2. 学费收取与续费流程

```mermaid
sequenceDiagram
    participant Student as 学员
    participant Receptionist as 前台
    participant PaymentGateway as 支付网关
    participant System as 系统
    
    Student ->> Receptionist: 申请续费/新购
    Receptionist ->> System: 查看收费标准
    System -->> Receptionist: 显示套餐信息
    Receptionist ->> Student: 介绍套餐
    Student ->> PaymentGateway: 支付
    PaymentGateway -->> System: 支付成功回调
    System ->> System: 更新学费记录
    System ->> System: 计算新的过期日期
    System -->> Receptionist: 续费成功
    Receptionist -->> Student: 提供收据
```

### 3. 课程参加流程

```mermaid
graph LR
    Student["学员"]
    
    subgraph PublicCourse["公共课程 - 免费"]
        CourseList["查看课程表"]
        SignUp["直接报名参加"]
        Attend["参加课程"]
    end
    
    subgraph PrivateCourse["私教课程 - 付费"]
        Browse["浏览私教课程"]
        Purchase["购买课程"]
        Payment["支付费用"]
        BookSession["预约课程"]
        AttendPrivate["参加私教课"]
    end
    
    Student --> CourseList
    CourseList --> SignUp
    SignUp --> Attend
    
    Student --> Browse
    Browse --> Purchase
    Purchase --> Payment
    Payment --> BookSession
    BookSession --> AttendPrivate
```

## 模块说明

### 1. 用户管理模块
- **功能**：创建、编辑、删除员工，管理员工权限
- **角色**：Admin, Manager
- **关键操作**：员工入职、权限分配、离职管理

### 2. 营地管理模块
- **功能**：多营地管理、房间管理、床位分配
- **角色**：Manager
- **关键操作**：营地信息维护、房间创建、床位状态跟踪

### 3. 学员管理模块
- **功能**：学员信息、身体数据跟踪、入住管理
- **角色**：Manager, Receptionist, Coach
- **关键操作**：
  - 学员注册与信息管理
  - 定期身体指标记录（体重、BMI、体脂率等）
  - 入住/退住管理
  - 学员状态跟踪

### 4. 课程管理模块
- **公共课程**（免费，所有人可参加）
  - 每日课程表展示
  - 固定课程时间安排
  - 参加人数统计
  - 角色：Coach 教练，Receptionist 前台
  
- **私教课程**（付费）
  - 私教课程管理
  - 课程购买与支付
  - 课时跟踪与完成情况
  - 角色：Coach, Receptionist

### 5. 菜谱与营养管理模块
- **功能**：菜谱管理、每日菜单制定、营养信息跟踪
- **角色**：Chef 厨师，Manager
- **关键操作**：
  - 菜谱数据库管理（成分、卡路里、宏观营养）
  - 每日菜单安排（与学员无直接关系）
  - 营养分析

### 6. 费用管理模块
- **功能**：学费收取、续费管理、收费标准维护
- **角色**：Receptionist, Manager
- **关键操作**：
  - 营地收费标准管理（支持多种套餐）
  - 学费收取（初次与续费）
  - 支付状态跟踪
  - 到期提醒与续费处理

### 7. 统计与报表模块
- **功能**：学员统计、营地统计、财务统计
- **角色**：Manager, Admin
- **关键指标**：
  - 学员相关：总人数、活跃人数、入住人数、平均体重、平均BMI
  - 营地相关：总床位、占用床位、入住率、收入、待收费用
  - 课程相关：课程参加率、私教销售情况
  - 费用相关：收入统计、待收费用、续费率

## 技术栈

- **前端**：Next.js 16, React 19, TypeScript, Tailwind CSS v4
- **数据库**：PostgreSQL (建议) / MongoDB
- **认证**：JWT + NextAuth.js
- **支付**：支付宝/微信支付 (集成建议)
- **缓存**：Redis (可选)
- **部署**：Vercel / 自建服务器

## API 设计原则

- RESTful API 设计
- JWT 令牌认证
- 基于角色的访问控制（RBAC）
- 请求/响应标准化
- 错误处理统一格式

## 安全考虑

1. 身份认证与授权
2. 数据加密（密码、敏感信息）
3. API 速率限制
4. SQL 注入防护
5. CORS 配置
6. 审计日志记录

## 扩展方向

1. 短信/微信通知提醒
2. 微信小程序学员端
3. 数据分析与可视化大屏
4. 人工智能健身建议
5. 设备数据集成（体脂秤、运动手环等）
